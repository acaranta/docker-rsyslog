---
kind: template
load: docker-build-multiarch.yaml
data:
  dockerfiledir: .
  hubimagename: docker-rsyslog
  imagetag: latest  
  branches: ['master', 'main']
  repotype: hub
  buildarch: linux/amd64
